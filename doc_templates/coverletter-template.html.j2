<!DOCTYPE html>
<html lang="{{ 'de-CH' if language == 'de' else 'en' }}">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Cover Letter - {{ profile.full_name }}</title>
  <style>
    :root {
      --primary: {{ theme.primary_hex }};
      --accent: {{ theme.accent_hex }};
      --ink: #111111;
      --muted: #444444;
      --rule: #d9d9d9;
    }
    body {
      margin: 0;
      font-family: "Helvetica Neue", Arial, sans-serif;
      color: var(--ink);
      line-height: 1.5;
      font-size: 12.5px;
      background: #ffffff;
    }
    .page {
      max-width: 820px;
      margin: 0 auto;
      padding: 20px 26px;
    }
    .sender {
      border-bottom: 1px solid var(--rule);
      padding-bottom: 8px;
      color: var(--muted);
      font-size: 11.5px;
    }
    .sender strong {
      color: var(--primary);
      font-size: 13px;
    }
    .recipient {
      margin-top: 20px;
      white-space: pre-line;
    }
    .meta {
      margin-top: 14px;
      text-align: right;
      color: var(--muted);
      font-size: 11.5px;
    }
    .subject {
      margin-top: 14px;
      color: var(--primary);
      font-size: 18px;
      font-weight: 700;
    }
    p {
      margin: 10px 0;
      text-align: left;
    }
    .signature {
      margin-top: 16px;
    }
  </style>
</head>
<body>
  <main class="page">
    <div class="sender">
      <strong>{{ profile.full_name }}</strong>
      {% if profile.location %} | {{ profile.location }}{% endif %}
      {% if profile.email %} | {{ profile.email }}{% endif %}
      {% if profile.phone %} | {{ profile.phone }}{% endif %}
    </div>

    <div class="recipient">
      {{ company_name }}
    </div>

    <div class="meta">
      {{ today }}
    </div>

    <h1 class="subject">
      {{ "Bewerbung als" if language == "de" else "Application for" }} {{ position_title }}
    </h1>

    <p>{{ content.cover_greeting }}</p>
    <p>{{ content.cover_opening }}</p>
    {% for paragraph in content.cover_body %}
      <p>{{ paragraph }}</p>
    {% endfor %}
    <p>{{ content.cover_closing }}</p>

    <div class="signature">
      <p>{{ "Freundliche Gruesse" if language == "de" else "Kind regards" }},</p>
      <p><strong>{{ profile.full_name }}</strong></p>
    </div>
  </main>
</body>
</html>

