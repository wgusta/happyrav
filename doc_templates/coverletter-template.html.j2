<!DOCTYPE html>
<html lang="{{ 'de-CH' if language == 'de' else 'en' }}">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Cover Letter - {{ profile.full_name }}</title>
  <style>
    @page { size: A4; margin: 20mm 18mm; }
    :root {
      --primary: {{ theme.primary_hex }};
      --accent: {{ theme.accent_hex }};
      --ink: #1a1d23;
      --muted: #555d6e;
      --rule: #d4d9e3;
    }
    * { box-sizing: border-box; }
    body { margin: 0; font-family: "Helvetica Neue", Arial, sans-serif; color: var(--ink); line-height: 1.56; font-size: 12px; background: #ffffff; -webkit-print-color-adjust: exact; print-color-adjust: exact; }
    .sender { border-bottom: 2px solid var(--primary); padding-bottom: 8px; margin-bottom: 16px; }
    .sender-name { color: var(--primary); font-size: 14px; font-weight: 700; }
    .sender-details { color: var(--muted); font-size: 10.5px; margin-top: 3px; }
    .recipient { line-height: 1.6; font-size: 11.5px; }
    .recipient strong { display: block; }
    .date-line { margin-top: 12px; text-align: right; color: var(--muted); font-size: 11px; }
    .subject { margin-top: 14px; color: var(--primary); font-size: 16px; font-weight: 700; }
    p { margin: 10px 0; }
    .signature { margin-top: 18px; }
    .signature-name { font-weight: 700; }
  </style>
</head>
<body>
  <main>
    <div class="sender">
      <div class="sender-name">{{ profile.full_name }}</div>
      <div class="sender-details">
        {% if profile.location %}{{ profile.location }}{% endif %}
        {% if profile.email %} · {{ profile.email }}{% endif %}
        {% if profile.phone %} · {{ profile.phone }}{% endif %}
      </div>
    </div>

    <div class="recipient">
      <strong>{{ company_name }}</strong>
    </div>

    <div class="date-line">{{ today }}</div>

    <h1 class="subject">{{ "Bewerbung als" if language == "de" else "Application for" }} {{ position_title }}</h1>

    <p>{{ content.cover_greeting }},</p>
    <p>{{ content.cover_opening }}</p>
    {% for paragraph in content.cover_body %}
      <p>{{ paragraph }}</p>
    {% endfor %}
    <p>{{ content.cover_closing }}</p>

    <div class="signature">
      <p>{{ "Freundliche Grüsse" if language == "de" else "Kind regards" }}</p>
      <p class="signature-name">{{ profile.full_name }}</p>
    </div>
  </main>
</body>
</html>
